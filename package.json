{
  "name": "webapp-serverside",
  "version": "1.0.0",
  "description": "Web App From Scratch refactored to render on the server side.",
  "main": "app.js",
  "engines": {
    "node": "7.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js && watchify main.js -o public/js/bundle.js -v",
    "build": "browserify main.js -o public/js/bundle.js -v",
    "watch": "watchify main.js -o public/js/bundle.js -v"
  },
  "author": "Tristan Jacobs",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^14.1.0",
    "watchify": "^3.9.0"
  },
  "dependencies": {
    "body-parser": "^1.17.1",
    "ejs": "^2.5.6",
    "express": "^4.15.2",
    "request": "^2.81.0",
    "watchify": "^3.9.0"
  }
}
